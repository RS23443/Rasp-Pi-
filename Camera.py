from picamera import PiCamera
from time import sleep
from libcamera import LibCamera
from gpiozero import Motor

#def variable
lback = motor(#pin1,pin2)
  rba
  

camera.start_preview()
sleep(5)
camera.stop_preview()

camera = PiCamera()
camera.resolution (1080,1080)
camera.start_preview()
#preview: open stream 
sleep (2)
camera.capture('uno.jpeg')

sleep (2)

sleep(2)
camera.capture('dos.jpeg')
camera.stop_preview()

f = open("uno.jpeg")
print(f)
g= open("dos.jpeg")

if f == g
  lback(0.25)
  
  
